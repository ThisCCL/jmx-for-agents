package io.github.thisccl.j4a.reference;

/**
 * Opaque request-local proof that a public component reference resolved exactly.
 *
 * <p>Implementations are owned by the bound reference module. Caller-created
 * implementations are not valid handles and never match a bound component.</p>
 */
public interface ResolvedNodeHandle {
}
