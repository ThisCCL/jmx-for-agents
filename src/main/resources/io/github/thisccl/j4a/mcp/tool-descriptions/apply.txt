Apply one ordered YAML patch using refs and non-empty native property-address arrays from read; string members are property/map keys and integer members are collection indexes. Runtime-proven rows execute in declaration order: append adds one row after the current final row, insert accepts an index from zero through the current row count plus one row, and remove requires an existing row index. Property types are exactly string|boolean|int|long|float|double|null|raw|collection|map|element|rows|opaque; float/double decode from the declared finite type and rows accepts a bare list or {row_type,row_properties,rows}. Supply exactly one file/path, one patchYaml/patchFile, and dryRun, override, or distinct out. Success returns declaration-ordered value-free changeResults; surviving adds also return createdRefs/resultRef with target-bound ref_scope. Usage/schema exits 2, accepted semantic failure exits 3, and infrastructure/fatal exits 4. Fatal is unknown outcome: inspect/read the target before any retry; the server never automatically replays a mutation.
