Create a minimal valid JMX through selected-runtime JMeter model objects. out is required. An existing target returns two advisory calls: retry with the original arguments plus forceOut:true, or replace only out with <different-output.jmx>; neither runs automatically. forceOut:true explicitly authorizes replacement. A successful receipt proves save/reload validation and returned refs are target-bound; after a changed target, read fresh refs.
