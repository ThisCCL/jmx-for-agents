Edit one existing property using a current source-bound locator plus a non-empty native address array copied from read; string members are property/map keys and integer members are collection indexes. Use exactly string|boolean|int|long|float|double|null|raw|collection|map|element|rows|opaque; finite float/double decode from the declared type and TestElement.enabled accepts boolean. Structured values require an explicit type; rows accepts a bare list or {row_type,row_properties,rows}. Use override only for the same input or out for a distinct copy. A successful receipt proves save/reload validation; unavailable refs require fresh same-file/same-home read, not replay.
