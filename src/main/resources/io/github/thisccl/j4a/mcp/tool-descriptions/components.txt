Discover selected-runtime exact component FQCNs and writable authoring metadata, including canonical boolean TestElement.enabled; generic TestBeans never expose TestBeanGUI as component. Property addresses are non-empty native arrays of string property/map-key segments and integer collection indexes. category plus details returns complete FQCN-ordered entries with limit 1..50, maxBytes 4096..65536, and a digest-bound cursor. Cursor v3 is rejected: restart at the first page. A COMPONENT_DETAIL_TOO_LARGE row supplies a signed componentToken; call components again with that sole selector (plus optional jmeter_home) for exact untruncated ordinary detail. Diagnostics emits exactly runtime-proven or runtime-metadata-unavailable for metadata-source availability, not GUI completeness; successful empty evidence is proven, while negative evidence can be observed again only after worker replacement. Calls do not open windows, run GUI-openability validation, execute samplers/load, or start the desktop GUI.
