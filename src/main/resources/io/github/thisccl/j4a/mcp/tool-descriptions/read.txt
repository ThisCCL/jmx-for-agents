Inspect one JMX tree without writing. Supply exactly one of file/path and optionally jmeter_home, depth, ref, properties, or includeDisabledDetails. MCP properties are none|key|all|writable; prefer writable for authoring. Property addresses are non-empty native arrays of string property/map-key segments and integer collection indexes. Values preserve declared float/double types and canonical boolean TestElement.enabled. Focus with ref to acquire a target-bound opaque envelope. Returned refs are opaque source-bound session handles; after eviction, stale state, or a changed target, read the same file/home again.
